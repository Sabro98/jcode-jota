export type JotaProblem = {
  code: string; // problems code
  is_pretesed: boolean;
  label: string;
  max_submissions: number;
  name: string; // problems name
  partial: boolean;
  points: number;
  contest: string;
};
